PORT=8080
DATA_DIR=/app/data
DOWNLOADS_DIR=/app/downloads
NODE_ENV=production

# Reverse proxy notes:
# - Terminate TLS at the proxy.
# - Protect the app with authentication before exposing it outside a private network.
# - Forward /api, /media, and the frontend routes to the same backend service.
